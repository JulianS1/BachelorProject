# R (Bachelor Project)

# ***Read***

The required libraries are:
    - brms
    - bartmachine
    - BayesPlot
    - ggplot2
    - ICEbox

To run:

    - change filepath at the start of each model to your dataset
    - load saved models with <- readRDS(<model name>)